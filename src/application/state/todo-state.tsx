import { Todo } from "../../domain/todo/todo";
import React from 'react';
import { useSelector, createStoreHook } from 'react-redux';
import { configureStore } from '@reduxjs/toolkit'

export default configureStore({
    reducer: {},
  })